module firefly-os.dev

go 1.22.0

// We do not use the Go tool to build this
// codebase. We use Bazel instead.
//
// See README.md.
//
// We only include this go.mod so that `godoc`
// works correctly.
