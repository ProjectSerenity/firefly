[package]
name = "kernel"
version = "0.1.0"
authors = ["Jamie Hall <the.sly.marbo@googlemail.com>"]
edition = "2018"

[dependencies]
bootloader = "0.9.5"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
