# build

build is a small set of helper libraries used in [`kbuild`](/kbuild). The functionality
here is only separated into another library so that it can be used both by kbuild and
`gen-version-data.go` (which is a one-file tool).
