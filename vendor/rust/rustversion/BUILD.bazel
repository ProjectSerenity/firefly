# Code generated by vendeps. DO NOT EDIT.

# Copyright 2022 The Firefly Authors.
#
# Use of this source code is governed by a BSD 3-clause
# license that can be found in the LICENSE file.

load("@rules_rust//cargo:defs.bzl", "cargo_build_script")
load("@rules_rust//rust:defs.bzl", "rust_doc", "rust_proc_macro", "rust_test")

rust_proc_macro(
    name = "rustversion",
    srcs = glob(["**/*.rs"]),
    compile_data = glob(["**"]),
    crate_name = "rustversion",
    edition = "2018",
    rustc_flags = [
        # In most cases, warnings in 3rd party crates are not interesting as
        # they're out of the control of consumers. The flag here silences
        # warnings. For more details see:
        # https://doc.rust-lang.org/rustc/lints/levels.html
        "--cap-lints=allow",
    ],
    tags = [
        "manual",  # Only build when needed as a dependency.
    ],
    version = "1.0.11",
    visibility = ["//visibility:public"],
    deps = [
        ":rustversion_build_script",
    ],
)

cargo_build_script(
    name = "rustversion_build_script",  # This is special-cased in Rustc, so don't change it.
    srcs = glob(["**/*.rs"]),
    crate_name = "build_script_build",
    crate_root = "build/build.rs",
    edition = "2018",
    rustc_flags = [
        # In most cases, warnings in 3rd party crates are not interesting as
        # they're out of the control of consumers. The flag here silences
        # warnings. For more details see:
        # https://doc.rust-lang.org/rustc/lints/levels.html
        "--cap-lints=allow",
    ],
    version = "1.0.11",
    visibility = ["//visibility:private"],
)

rust_doc(
    name = "docs",
    crate = ":rustversion",
    visibility = ["//visibility:public"],
)

rust_test(
    name = "rustversion_test",
    size = "small",
    compile_data = glob(["**"]),
    crate = ":rustversion",
    crate_name = "rustversion",
    rustc_flags = [
        # In most cases, warnings in 3rd party crates are not interesting as
        # they're out of the control of consumers. The flag here silences
        # warnings. For more details see:
        # https://doc.rust-lang.org/rustc/lints/levels.html
        "--cap-lints=allow",
    ],
)
