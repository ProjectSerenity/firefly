(package printer)

(let Default-message "Hello, world!\n")

'(param (msg string) rsi)
'(param (msg-len int) rdx)
(asm-func Print
	(mov eax 1)                            ; sys_write
	(mov rdi 1)                            ; unsigned int fd stdout
	(syscall)                              ; write(1, "Hello, world!\n", 14)

	(ret))
